{
  "files": [
    {
      "pattern": "$(Build.ArtifactStagingDirectory)\\zipped\\*.*",
      "target": "corporativo-deploy/$(System.TeamProject)/$(Build.Repository.Name)/$(Build.BuildNumber)/"
    }
  ]
}